- Pengenalan Github 
  ^ GitHub adalah layanan hosting berbasis cloud yang memungkinkan pengembang untuk menyimpan, berbagi, dan berkolaborasi dalam menulis kode.
- Istilah istilah dalam GitHub
 * Repository 
 * Merge 
 * Commit
 * Pull Requset 
 * Branch 
 * Merge Conflict

- Git 
  * Merupakan sistem Version Control System yang digunakan untuk melacak perubahan 
  * Git Command 
    ^ git init = menginisialisasi repository git dalam folder 
    ^ git add = menambah file ke staging 
    ^ git status 
    ^ git commit = menyimpan perubahan
    ^ git config 
    ^ git branch 
    ^ git help
  * Working Space pada Git
    ^ Working tree
      - git add 
      - git status
    ^ Staging area 
      - git commit 
    ^ History
      # Staging area dan History akan masuk ke dalam folder .git
